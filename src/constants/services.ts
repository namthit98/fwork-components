import { BLUE_GRAY, BROWN } from './colors'
import { IServiceColor } from './interfaces'

export const DEFAULT: IServiceColor = {
  primary: BLUE_GRAY[700],
  secondary: BLUE_GRAY[600],
}

export const PRODUCT: IServiceColor = {
  primary: BROWN[500],
  secondary: BROWN[300],
}
